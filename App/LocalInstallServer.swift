// LocalInstallServer removed — replaced with a harmless stub to avoid accidental diff markers in the file.
// The original local loopback install server was removed in favor of the R2 upload + presigned manifest flow.
// Keeping this stub file (instead of deleting) to avoid CI issues; it has no runtime effect.
import Foundation

// Intentionally empty implementation.
